Thank you for installing {{ .Chart.Name }}!

Your S3-compatible storage server is now being deployed.

Release name: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

1. Check the status of the deployment:
   kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "go-faster-fs.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

2. Access the S3 service:
{{- if .Values.ingress.enabled }}
   The service is available via Ingress at:
   {{- range .Values.ingress.hosts }}
   http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
   {{- end }}
{{- else if .Values.httpRoute.enabled }}
   The service is available via HTTPRoute at:
   {{- range .Values.httpRoute.hostnames }}
   http://{{ . }}
   {{- end }}
{{- else }}
   kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "go-faster-fs.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
   # Then access http://127.0.0.1:{{ .Values.service.port }}
{{- end }}

3. Access the pprof endpoint:
   kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "go-faster-fs.fullname" . }} 9010:9010
   # Then visit http://127.0.0.1:9010/debug/pprof/

4. Configure your S3 client:
   Endpoint URL: http://{{ include "go-faster-fs.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

   Example AWS CLI configuration:
   aws configure set default.s3.endpoint_url http://{{ include "go-faster-fs.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

   Example environment variables for SDK:
   export AWS_ENDPOINT_URL=http://{{ include "go-faster-fs.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
   export AWS_ACCESS_KEY_ID=test
   export AWS_SECRET_ACCESS_KEY=test

For more information, visit: https://github.com/go-faster/fs
