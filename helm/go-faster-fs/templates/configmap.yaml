apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "go-faster-fs.fullname" . }}-config
  labels:
    {{- include "go-faster-fs.labels" . | nindent 4 }}
data:
  config.yaml: |
{{- .Values.config | toYaml | nindent 4 }}
