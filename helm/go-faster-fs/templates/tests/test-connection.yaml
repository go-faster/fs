apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-faster-fs.fullname" . }}-test-connection"
  labels:
    {{- include "go-faster-fs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-faster-fs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

