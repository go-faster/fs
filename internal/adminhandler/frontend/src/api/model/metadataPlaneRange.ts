/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * go-faster/fs admin API
 * Administrative API for a go-faster/fs S3 server: instance info and runtime management of access keys (create, list and delete credentials without a restart). Intended to be served on a separate, authenticated admin listener.

 * OpenAPI spec version: 0.1.0
 */
import type { MetadataPlaneRangeStatus } from './metadataPlaneRangeStatus';

export interface MetadataPlaneRange {
  /** First key in the range, inclusive. Empty is the start of the key space. */
  start: string;
  /** First key past the range, exclusive. Empty is the end of the key space. */
  end: string;
  /** Node serving reads and applying writes for this range. */
  owner: string;
  /** Nodes receiving the owner's log, ready to be promoted. */
  followers?: string[];
  /** Nodes being copied into, which must never be promoted: they hold only part of the range. */
  learners?: string[];
  /** Node this range is being handed to, when a move is in flight. */
  move_to?: string;
  status: MetadataPlaneRangeStatus;
}
