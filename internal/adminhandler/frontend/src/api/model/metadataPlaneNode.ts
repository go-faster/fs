/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * go-faster/fs admin API
 * Administrative API for a go-faster/fs S3 server: instance info and runtime management of access keys (create, list and delete credentials without a restart). Intended to be served on a separate, authenticated admin listener.

 * OpenAPI spec version: 0.1.0
 */

export interface MetadataPlaneNode {
  id: string;
  /** Whether the node is registered in the control plane. */
  live: boolean;
  /** Whether the node answered; the rest of its fields are meaningless when false. */
  reporting: boolean;
  /** Map revision this node is routing by. Behind the plane revision means lazy routing has left it stale. */
  revision?: number;
  /** Whether this node is routing by an older revision than the control plane holds. */
  behind?: boolean;
  /** Ranges this node serves. */
  owned?: number;
  /** Ranges this node holds for another node, as a follower or a learner. */
  replicated?: number;
}
